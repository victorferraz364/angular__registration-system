export const options = {
    tooltip: {
      textStyle: {
        color: '#FF5733' // Altera a cor do texto dentro do tooltip (input do seletor)
      }
    },
    legend: {
      textStyle: {
        color: '#FF5733' // Altera a cor do texto no seletor da legenda
      }
    }
  };
  